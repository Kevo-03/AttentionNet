@startuml
participant "Training Controller" as TC
participant "Learning Rate Scheduler" as Sched
participant "Optimization Module" as Opt

alt During warmup phase
    TC -> TC: Calculate warmup progress ratio\nbased on current epoch
    TC -> TC: Compute progressively increasing\nlearning rate value
    TC -> Opt: Apply updated learning rate
else After warmup phase
    TC -> Sched: Advance learning rate schedule\nusing cosine warm restarts
    Sched --> TC: Learning rate may change\naccording to restart cycle
end
@enduml